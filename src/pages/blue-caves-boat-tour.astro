---
import SEOHead from '../components/SEOHead.astro';
import BlueCavesBody from '../components/pages/BlueCavesBody.astro';
import { blueCavesTour } from '../i18n/pages/blue-caves-boat-tour';

const lang = 'en';
const s = blueCavesTour[lang];
const siteUrl = 'https://rentaboatzakynthos.com';
const path = '/blue-caves-boat-tour';

const schema = {
	'@context': 'https://schema.org',
	'@graph': [
		{
			'@type': 'TouristTrip',
			name: s.heroTitle,
			description: s.seoDescription,
			url: `${siteUrl}${path}`,
			touristType: ['Beach lovers', 'Couples', 'Families'],
			provider: { '@id': `${siteUrl}/#business` },
			itinerary: {
				'@type': 'ItemList',
				itemListElement: [
					{ '@type': 'ListItem', position: 1, item: { '@type': 'TouristAttraction', name: 'Blue Caves', address: { '@type': 'PostalAddress', addressLocality: 'Zakynthos', addressCountry: 'GR' } } },
				],
			},
		},
		{
			'@type': 'BreadcrumbList',
			itemListElement: [
				{ '@type': 'ListItem', position: 1, name: 'Home', item: siteUrl },
				{ '@type': 'ListItem', position: 2, name: 'Recommended Routes', item: `${siteUrl}/recommended-routes` },
				{ '@type': 'ListItem', position: 3, name: s.heroTitle, item: `${siteUrl}${path}` },
			],
		},
	],
};
---

<html lang={lang}>
	<head>
		<SEOHead
			title={s.seoTitle}
			description={s.seoDescription}
			lang={lang}
			basePath={path}
			translated={true}
			ogImage="https://rentaboatzakynthos.com/images/IMG_6507.jpeg"
		/>
		<script type="application/ld+json" set:html={JSON.stringify(schema)} />
	</head>
	<BlueCavesBody lang={lang} />
</html>
